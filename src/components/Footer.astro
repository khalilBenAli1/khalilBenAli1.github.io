<footer class="footer">
  <div class="footer__inner">
    <h2>Let's build.</h2>
    <p class="footer__statement">
      The interesting problems are the ones that span systems.
    </p>

    <nav class="footer__links" aria-label="Contact links">
      <a href="mailto:khalil.benali16121@gmail.com">Email</a>
      <a href="https://github.com/khalilBenAli1">GitHub</a>
      <a href="https://www.linkedin.com/in/khalil-ben-ali">LinkedIn</a>
      <a href="/KhalilBenAli-CV.pdf">Résumé (PDF)</a>
    </nav>

    <div class="footer__bottom">
      <p class="footer__polyglot">
        HELLO · BONJOUR · <span lang="ar" dir="rtl">أهلاً</span> · CIAO
      </p>
      <p class="footer__colophon">
        Designed &amp; built by Khalil Ben Ali — Astro, GSAP, canvas, and a
        two-model AI workflow. Type: Bricolage Grotesque, Instrument Sans, IBM
        Plex Mono.
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding-block: clamp(80px, 12vw, 152px) 40px;
    background: var(--ink);
    color: var(--paper);
  }

  .footer__inner {
    width: min(
      calc(100% - var(--gutter) - var(--gutter)),
      var(--container)
    );
    margin-inline: auto;
  }

  .footer h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(4rem, 14vw, 12rem);
    font-weight: 650;
    line-height: 0.76;
    letter-spacing: -0.065em;
  }

  .footer__statement {
    max-width: 620px;
    margin: clamp(40px, 7vw, 72px) 0 0;
    font-size: clamp(1.15rem, 2vw, 1.5rem);
    line-height: 1.5;
  }

  .footer__links {
    display: flex;
    flex-wrap: wrap;
    gap: 16px 32px;
    margin-block-start: 40px;
  }

  .footer__links a {
    position: relative;
    color: var(--paper);
    font-family: var(--font-mono);
    font-size: 0.84rem;
    font-weight: 500;
    line-height: 1.4;
    text-decoration: none;
  }

  .footer__links a::after {
    position: absolute;
    right: 0;
    bottom: -5px;
    left: 0;
    height: 1px;
    background: currentColor;
    content: "";
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 220ms ease;
  }

  .footer__links a:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }

  .footer__links a:focus-visible {
    outline: 3px solid var(--paper);
    outline-offset: 6px;
  }

  .footer__bottom {
    display: grid;
    grid-template-columns: auto minmax(0, 650px);
    gap: 32px;
    justify-content: space-between;
    align-items: end;
    margin-block-start: clamp(72px, 12vw, 136px);
    padding-block-start: 24px;
    border-block-start: 1px solid color-mix(in srgb, var(--paper) 30%, transparent);
  }

  .footer__polyglot,
  .footer__colophon {
    margin: 0;
    font-family: var(--font-mono);
    font-size: 0.68rem;
    line-height: 1.6;
    letter-spacing: 0.035em;
  }

  .footer__polyglot {
    white-space: nowrap;
  }

  .footer__polyglot :lang(ar) {
    display: inline-block;
    font-family: var(--font-arabic);
    font-weight: 400;
    letter-spacing: 0;
  }

  .footer__colophon {
    max-width: 650px;
  }

  @media (max-width: 760px) {
    .footer__bottom {
      grid-template-columns: 1fr;
      align-items: start;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .footer__links a::after {
      transform: none;
      opacity: 0;
      transition: none;
    }

    .footer__links a:hover::after {
      opacity: 1;
    }
  }
</style>
