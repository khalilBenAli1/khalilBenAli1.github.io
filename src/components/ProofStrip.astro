<aside class="proof-strip" aria-label="Production proof points">
  <ul class="proof-strip__list">
    <li>3,800+ commits on one production platform</li>
    <li>10 SaaS systems wired into one hub</li>
    <li>129/129 commits — sole dev, AI brand app</li>
    <li>5 stacks in production: TS · C# · C++ · SQL · Python</li>
  </ul>
</aside>

<style>
  .proof-strip {
    border-block: 1px solid var(--cobalt);
    color: var(--cobalt);
  }

  .proof-strip__list {
    width: min(
      calc(100% - var(--gutter) - var(--gutter)),
      var(--container)
    );
    margin: 0 auto;
    padding: 20px 0;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    list-style: none;
    font-family: var(--font-mono);
    font-size: clamp(0.68rem, 0.9vw, 0.78rem);
    font-weight: 500;
    line-height: 1.45;
    letter-spacing: 0.02em;
  }

  .proof-strip__list li {
    padding-inline: 20px;
    border-inline-start: 1px solid var(--cobalt);
  }

  .proof-strip__list li:first-child {
    padding-inline-start: 0;
    border-inline-start: 0;
  }

  .proof-strip__list li:last-child {
    padding-inline-end: 0;
  }

  @media (max-width: 840px) {
    .proof-strip__list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0;
      padding-block: 8px;
    }

    .proof-strip__list li {
      padding: 14px;
      border-inline-start: 0;
      border-block-end: 1px solid var(--cobalt);
    }

    .proof-strip__list li:nth-child(odd) {
      padding-inline-start: 0;
      border-inline-end: 1px solid var(--cobalt);
    }

    .proof-strip__list li:nth-last-child(-n + 2) {
      border-block-end: 0;
    }
  }

  @media (max-width: 480px) {
    .proof-strip__list {
      grid-template-columns: 1fr;
    }

    .proof-strip__list li,
    .proof-strip__list li:nth-child(odd) {
      padding: 12px 0;
      border-inline-end: 0;
      border-block-end: 1px solid var(--cobalt);
    }

    .proof-strip__list li:last-child {
      border-block-end: 0;
    }
  }
</style>
